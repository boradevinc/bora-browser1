name: Build Bora Browser

on:
  push:
    tags:
      - 'v*'

# Şurayı komple değiştir kanka:
permissions:
  contents: write
  id-token: write

jobs:
  build:
    name: Build Bora Browser
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Build Process
        run: |
          echo "Bora Browser derleme islemi basliyor..."
          mkdir -p build_output
          echo "Bora Browser Build v1.0" > build_output/Bora_Browser_Info.txt
          
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: build_output/*
          # Token'ı otomatik yönetmesi için burayı boş bırakabilirsin
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
